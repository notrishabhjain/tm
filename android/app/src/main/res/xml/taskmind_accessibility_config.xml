<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuration for TaskMind's UI-automation service.

  canRetrieveWindowContent is required to find controls by label; the service
  reads window content only while a user-initiated run is executing.

  No accessibilityEventTypes are requested: this service is a driver, not an
  observer. It never receives a stream of what the user is doing — it only reads
  the foreground window during a run the user started.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/taskmind_accessibility_description" />
